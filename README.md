# Compile
javac -cp "lib/json-20231013.jar" -d out src/*.java src/model/*.java

# Run
java -cp "out:lib/json-20231013.jar" Main

Validate syntax → Option 1

View structure → Option 2

Convert to JSON → Option 3
